; Place this file in the DOCUMENT ROOT (/home/goayu/samutkarshias) on the server.
; Raises the host's tight defaults so admin image uploads and setup don't choke.
; Changes apply within ~5 min (user_ini.cache_ttl). Or use cPanel MultiPHP INI Editor.
upload_max_filesize = 16M
post_max_size = 20M
memory_limit = 256M
max_execution_time = 120
max_input_time = 120
